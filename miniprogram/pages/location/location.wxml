<view>
    <view class="theMap">
        <map id="theMap" longitude="{{longitude}}" latitude="{{latitude}}" scale="{{scale}}" markers="{{markers}}" bindcontroltap="controlTap"></map>
    </view>
</view>